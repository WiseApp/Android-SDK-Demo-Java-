pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // Warning: this repository is going to shut down soon

        // START Wise SDK
        maven {
            url "https://wise-maven.s3.ap-south-1.amazonaws.com/android/snapshots"
        }
        // END Wise SDK
    }
}
rootProject.name = "SDK Demo Java"
include ':app'
include ':commonlib'
include ':mobilertc'
